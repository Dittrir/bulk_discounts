<h1>Discount #: <%= @discount.id %></h1>
<hr>
<p>This discount takes <%= @discount.percent_discount %>% off purchases of <%= @discount.quantity_threshold %> or more items!</p>
<p><%= link_to "Check it out here!", "/merchants/#{@merchant.id}/bulk_discounts/#{@discount.id}"%>
<p><%= link_to "Edit this discount", edit_merchant_bulk_discount_path(@merchant, @discount)%>
